import { Typography } from "antd";

const { Title } = Typography;

export default function ContextAlert() {
  return (
    <main className="flex flex-col items-center justify-center gap-2 h-screen">
      <Title level={2}>TimeLink</Title>
    </main>
  );
}
